services:
  - type: web
    name: wallet-service
    runtime: docker
    plan: free
    rootDir: wallet-service
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOW_TEST_FUND
        value: "0"
